# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri build artifacts
src-tauri/target
src-tauri/gen/schemas

# Raw design assets (large; app bundles scaled copies from src/assets)
ماهی قرمز/
ماهی فریمز/
MAHI icon.png

# Local agent config
.claude/

# Test audio generated while developing the ElevenLabs TTS integration
.mahi-speech/

# Local version-history log Claude maintains for its own version-numbering
# decisions — not meant to be part of the repo's shared history
VERSION_LOG.md

# Local-only infrastructure credentials (server logins etc) — never commit
PROJECT_CREDENTIALS.md

# Updater signing private key — never commit (the .pub counterpart is fine,
# it's referenced from tauri.conf.json)
src-tauri/mahi-updater.key
